%li.span4
  %div.group-card.thumbnail[group.model]
    = group.delete_link

    = group.logo_link
    .caption
      %p= group.name_link
      %p= group.url_2_link
      %p= group.email_link
      %p.description= group.description
      = render 'tags', tags: group.tags
